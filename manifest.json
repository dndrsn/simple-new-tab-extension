{
	"name": "Simple new tab page",
	"description": "Override the new tab page with a simple one",
	"version": "0.1",
	"incognito": "split",
	"chrome_url_overrides": {
		"newtab": "index.html"
	},
	"permissions": [
		"bookmarks",
		"chrome://favicon/"
	],
	"background": {
    	"scripts": ["js/background.js"]
  	},
	"manifest_version": 2
}
